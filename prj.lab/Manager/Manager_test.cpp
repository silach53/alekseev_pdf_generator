#include<iostream>
#include<Manager/Manager.h>

int main()
{	
	for (size_t i = 0; i < 1000000; i++)
	{
		if (i%1000==0)
		{
			std::cout << i << std::endl;
		}
		uint64_t d = EmptyHandler;
		uint64_t* p = &d;
		hold a = (QueueP_manager::getInstance()).makeQueueP();
	}
	

}